# Universal Eden MCP Server Configuration
# Copy this file to .env and modify as needed

# Network Configuration
# HTTP port for REST API and SSE endpoints
HTTP_PORT=3001

# WebSocket port for real-time communication
WS_PORT=3002

# Host to bind to (use 0.0.0.0 for all interfaces)
HOST=localhost

# Examples:
# To avoid port conflicts, you can use different ports:
# HTTP_PORT=4000
# WS_PORT=4001

# To make the server accessible from other machines:
# HOST=0.0.0.0

# Usage:
# HTTP_PORT=4000 WS_PORT=4001 HOST=0.0.0.0 node server.js --http
