# Universal File Reader MCP Server Configuration
# Copy this file to .env and modify as needed

# File size limit (in bytes)
# Default: 50MB = 50 * 1024 * 1024
MAX_FILE_SIZE=52428800

# Allowed directories (comma-separated)
# Only files in these directories can be accessed
# Default: home directory and current directory
ALLOWED_DIRECTORIES=/Users/emmar,/c:/Echolace_Obsidian,/c:/Eden_Codeblocks

# Examples:
# To increase file size limit to 100MB:
# MAX_FILE_SIZE=104857600

# To allow access to specific directories:
# ALLOWED_DIRECTORIES=/path/to/docs,/path/to/projects,/Users/username

# For Windows paths, use forward slashes or escaped backslashes:
# ALLOWED_DIRECTORIES=C:/Users/username/Documents,D:/Projects

# Security Note:
# - Always restrict access to specific directories
# - Never use ALLOWED_DIRECTORIES=/ (root access)
# - Keep file size limits reasonable to prevent memory issues
